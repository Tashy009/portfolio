const projectsProps = {
    id: "projects",
    isDark: false,
    isRed: true
}

const aboutProps = {
    id: "about",
    isDark: false,
    isRed: false
}

const contactProps = {
    id: "contact",
    isDark: true,
    isRed: false
}

const homeProps = {
    id: "home",
    isDark: false,
    isRed: false
}

module.exports = {projectsProps, aboutProps, contactProps, homeProps}